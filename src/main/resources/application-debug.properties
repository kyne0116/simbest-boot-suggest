# 调试配置文件

# 启用调试日志
logging.level.com.simbest.boot.suggest=DEBUG

# 显示SQL语句
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 禁用缓存
spring.thymeleaf.cache=false
spring.resources.chain.cache=false

# 数据库配置
spring.datasource.url=jdbc:mysql://10.92.82.149:8430/suggest?useSSL=false&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&allowPublicKeyRetrieval=true
spring.datasource.username=suggestusr
spring.datasource.password=suggestPWD@2025
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA配置
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.connection.useUnicode=true

# 数据初始化配置
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true

# 历史匹配配置
historical.matching.similarity.threshold=0.5
historical.matching.max.results=10
historical.matching.confidence.threshold=0.4
historical.matching.use.patterns=true
